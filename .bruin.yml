default_environment: default
environments:
    default:
        connections:
            google_cloud_platform:
                - name: gcp-kickstarter
                  project_id: ${GCP_PROJECT_ID}
                  location: "EU"
                  service_account_file: ./secrets/sa.json
